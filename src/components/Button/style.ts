import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
container: {
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 12,
    backgroundColor: '#000',
    padding: 22,
    borderRadius: 8,
},
icon: {
    color: '#fff',
    fontSize: 24,
},
text: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',

},
})