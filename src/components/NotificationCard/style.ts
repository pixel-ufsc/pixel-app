import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    flexDirection: 'row',
    gap: 10,
    width: '100%',
    alignItems: 'center',
    paddingVertical: 10,
  },
  image: {
    width: 48,
    height: 48,
    borderRadius: 24,
    backgroundColor: '#693274',
  },    
});
